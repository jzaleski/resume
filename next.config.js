/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "resume.local",
    "resume-full.local",
    "resume-lite.local",
    "resume-recent.local",
  ],
};

export default nextConfig;
